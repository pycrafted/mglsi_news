# Fichiers d'environnement
# On garde un exemple mais on ne partage pas les vraies variables d'environnement
.env
.env.*
!.env.example

# Dossiers de dépendances
# Ces dossiers sont volumineux et peuvent être recréés avec composer/npm
/vendor/
/node_modules/
composer.lock
package-lock.json

# Fichiers de logs et cache
# Ces fichiers sont générés automatiquement et peuvent être recréés
/logs/
*.log
*.cache
*.tmp

# Fichiers système
# Ces fichiers sont créés automatiquement par votre système d'exploitation
.DS_Store
Thumbs.db
desktop.ini

# Fichiers de configuration d'éditeurs
# Chacun utilise son propre éditeur, pas besoin de partager ces fichiers
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
*.swp
*.swo

# Fichiers générés
# Ces fichiers sont créés lors de la compilation
/build/
/dist/
*.min.js
*.min.css

# Fichiers de base de données
# On ne partage pas les données, seulement la structure
*.sql
*.sqlite
*.db

# Sauvegardes
# Les fichiers de backup sont locaux
*.bak
*.backup
*~

# Fichiers temporaires d'upload
# Ces fichiers sont créés pendant l'upload
/uploads/temp/
*.tmp

# Configuration XAMPP
# Ces dossiers sont spécifiques à votre installation
xampp/
apache/
mysql/

# Sessions PHP
# Les fichiers de session sont personnels
sessions/
*.session

# Cache PHP
# Le cache est régénéré automatiquement
*.cache.php
opcache/

# Tests
# Les rapports de test sont générés localement
/tests/coverage/
.phpunit.result.cache

# Documentation générée
# On ne partage que le code source de la documentation
/docs/_build/
/docs/_static/
/docs/_templates/

# Configurations personnalisées
# Chacun a ses propres paramètres
config.local.php
config.prod.php
config.dev.php

# Médias uploadés
# On garde le dossier mais pas son contenu
/images/uploads/*
!/images/uploads/.gitkeep

# Cache des images
# Les images en cache sont générées automatiquement
/images/cache/*
!/images/cache/.gitkeep 